---
title: "distance - Tinkr API Documentation"
url: "https://docs.tinkr.site/Routine/Conditions/distance/"
scraped_at: "2025-06-30T02:53:47.070Z"
---

# distance

Returns the distance between two units.

```lua
distance(unita, unitb) : integerdistance(unita, unitb) : integer
```

Example

```lua
if distance('player', 'target') > 5 then    -- do ranged combatendif distance('player', 'target') > 5 then
    -- do ranged combat
end
```